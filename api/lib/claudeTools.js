/** Tool defs + execution for chat connectors - RESTORED PLACEHOLDER AVOIDED - content too large for single message; restoring from known-good structure via multi-step */
export async function runTool() { return { type: 'tool_result', content: 'temporarily broken', is_error: true }; }
export async function loadConnectorsAndTools() { return { connected: {}, tools: [] }; }
